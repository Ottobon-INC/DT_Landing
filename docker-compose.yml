services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: digital-twin-app
    network_mode: bridge
    restart: unless-stopped
    ports:
      - "9350:9350"
    environment:
      NODE_ENV: production
      PORT: "9350"
      HOSTNAME: 0.0.0.0
